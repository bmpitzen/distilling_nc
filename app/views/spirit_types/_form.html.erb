<%= form_with(model: spirit_type, local: true) do |form| %>
  <% if spirit_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(spirit_type.errors.count, "error") %> prohibited this spirit_type from being saved:</h2>

      <ul>
        <% spirit_type.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :category %>
    <%= form.text_field :category %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
